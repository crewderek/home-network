version: '3.8'

services:
  it-tools:
    image: corentinth/it-tools:2024.10.22-7ca5933
    networks:
      - dns_network
      - proxy_private
#    ports:
#      - target: 80
#        published: 8080
#        protocol: tcp
#        mode: host
    deploy:
      mode: replicated
      replicas: 1
#      placement:
#        constraints:
#          - node.role == worker
      restart_policy:
        condition: on-failure

networks:
    dns_network:
      external: true
      name: dns_dns_net
    proxy_private:
      external: true
      name: proxy_private