version: '3.8'

services:
  nginx_proxy_manager:
    image: 'jc21/nginx-proxy-manager:2.14.0'
    networks:
      - proxy_public
      - proxy_private
      - maria_db_net
    ports:
      - target: 80
        published: 18000
        protocol: tcp
        mode: ingress
      - target: 443
        published: 14443
        protocol: tcp
        mode: ingress
      - target: 81
        published: 81
        protocol: tcp
        mode: ingress
    volumes:
      - /mnt/docker/node_proxy_manager/data:/data
      - /mnt/docker/node_proxy_manager/letsencrypt:/etc/letsencrypt
    environment:
      DB_MYSQL_HOST: "maria-db"
      DB_MYSQL_PORT: 3306
      DB_MYSQL_USER: "node_proxy_manager"
      DB_MYSQL_PASSWORD: /run/secrets/maria_db_user_node_proxy_manager_password
      DB_MYSQL_NAME: "node_proxy_manager"
      PUID: 1000
      PGID: 1000
      TZ: America/Denver
    deploy:
      placement:
        constraints: [node.role == manager]

networks:
  proxy_public:
    driver: overlay
    attachable: true
    name: proxy_public
  proxy_private:
    driver: overlay
    attachable: true
    name: proxy_private
  maria_db_net:
    external: true
    name: database_maria_db_net

#volumes:
#  nginx_data:
#    driver: local
#    driver_opts:
#      type: nfs
#      o: "addr=${NFS_ADDRESS},nfsvers=4,nolock,soft,rw" # Replace with your NFS Server IP
#      device: ":${PROXY_DATA_NFS_DIRECTORY}"